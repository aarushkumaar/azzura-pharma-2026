// ============================================================
// env-config.example.js — COMMITTED TO GITHUB (template only)
// Copy this file to env-config.js and fill in your real values.
// env-config.js is gitignored and must never be committed.
//
// HOW TO SET UP LOCALLY:
//   1. Copy: cp env-config.example.js env-config.js
//   2. Open env-config.js and replace the placeholder values
//   3. env-config.js is loaded as the first script on every page
// ============================================================

window.__ENV__ = {
  RAZORPAY_KEY_ID:    'rzp_live_YOUR_KEY_HERE',
  SUPABASE_URL:       'https://YOUR_PROJECT.supabase.co',
  SUPABASE_ANON_KEY:  'YOUR_ANON_KEY_HERE'
};
